import type { NextConfig } from "next";

const nextConfig = {
  output: "export",
  images: { unoptimized: true },
  basePath: "/minjikim19.github.io",
  assetPrefix: "/minjikim19.github.io/",
};

module.exports = nextConfig;
